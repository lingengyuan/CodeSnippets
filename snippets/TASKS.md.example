# TASKS.md 示例
# 配合 python/mini_symphony.py 使用
# 格式：- [ ] 任务标题（缩进行为描述）

- [ ] Fix login timeout bug
      用户反映登录后 30 分钟自动退出，session TTL 配置有问题。
      相关文件: auth/session.py

- [ ] Add rate limiting to POST /api/v1/complete
      目前没有限流，容易被滥用。
      要求: 每 IP 每分钟最多 20 次请求，超出返回 429。

- [ ] Write unit tests for payment module
      payment/processor.py 目前 0 测试覆盖。
      至少覆盖: 成功支付、余额不足、网络超时三个场景。

- [x] Update README with install instructions
      已完成，跳过。
